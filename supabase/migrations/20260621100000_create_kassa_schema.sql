-- Bosqich 2: kassa moduli uchun alohida schema (public.patients bilan nom ziddiyati yo'q)
create schema if not exists kassa;

comment on schema kassa is 'Kassa tizimi jadvallari (Prisma: prisma/kassa/schema.prisma)';
